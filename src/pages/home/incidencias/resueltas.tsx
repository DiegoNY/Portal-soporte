import { LayoutPrincipal } from "@/components/LayoutPrincipal";

const Resueltas = () => {
  return (
    <LayoutPrincipal>
      <div>resueltas</div>
    </LayoutPrincipal>
  );
};
export default Resueltas;
